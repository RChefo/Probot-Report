# دليل إعداد نظام المراقبة (Webhook)

## 📋 ما هو نظام الـ Webhook؟

نظام مراقبة متقدم يرسل جميع الأخطاء والمشاكل التي تحدث في البوت مباشرة إلى Discord عبر webhook خاص.

## 🚀 كيفية الإعداد:

### 1. إنشاء Webhook في Discord:
```
1. اذهب إلى السيرفر الخاص بك
2. اضغط كليك يميني على روم → Edit Channel
3. اذهب لقسم "Integrations" → Webhooks
4. اضغط "Create Webhook"
5. أدخل اسم مثل "ProBot Error Logger"
6. اختر الروم الذي تريد إرسال الأخطاء إليه
7. اضغط "Copy Webhook URL"
```

### 2. تفعيل Webhook في البوت:
```
/setwebhook https://discord.com/api/webhooks/123456789012345678/abcdef123456789
```

### 3. اختبار النظام:
بعد التفعيل، سيرسل البوت رسالة اختبار إلى الـ webhook.

## 📊 ما يتم تسجيله:

### ✅ أنواع الأخطاء:
- أخطاء معالجة التقارير
- أخطاء البحث في قاعدة البيانات
- أخطاء التحديثات التلقائية
- أخطاء معالجة الأزرار والنماذج
- أخطاء الاتصال بالديسكورد

### 📋 معلومات الخطأ:
```
🚨 Bot Error Report

🔴 Error Type: ReferenceError
📝 Error Message: userId is not defined
📍 Location: handleReportModal
⏰ Time: [timestamp]

📋 Stack Trace:
[تفاصيل الخطأ الكاملة]

📊 Additional Info:
userId: 123456789012345678
action: create_report
```

## 🎯 الفوائد:

- **🔍 مراقبة فورية**: تعرف بأي مشكلة فور حدوثها
- **📈 تحسين الأداء**: حل المشاكل قبل أن تؤثر على المستخدمين
- **📊 إحصائيات**: تتبع أداء البوت ومعدلات الأخطاء
- **🛡️ أمان**: webhook محمي ولا يمكن الوصول إليه إلا من البوت

## ⚙️ إدارة الـ Webhook:

### تعديل الرابط:
```
/setwebhook https://discord.com/api/webhooks/NEW_WEBHOOK_URL
```

### إلغاء التفعيل:
```
/setwebhook
```
(اتركه فارغاً للإلغاء)

## 🔒 الأمان:

- ✅ **Webhook محمي**: لا يمكن الوصول إليه إلا من البوت
- ✅ **معلومات آمنة**: لا يتم مشاركة بيانات حساسة
- ✅ **صلاحيات محدودة**: webhook للقراءة فقط
- ✅ **تشفير**: جميع البيانات مشفرة عبر HTTPS

## 📞 دعم فني:

إذا واجهت مشاكل في إعداد الـ webhook:
1. تأكد من صحة رابط الـ webhook
2. تأكد من صلاحيات البوت في الروم
3. جرب إعادة إنشاء webhook جديد
4. تأكد من أن البوت متصل بالإنترنت

---

**نظام المراقبة جاهز للحماية! 🛡️✨**
